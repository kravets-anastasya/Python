Задание 3 Подсчитать количество целых чисел в диапазоне от 100 до 9999 у которых все цифры разные.

counter = 0 #счетчик

for i in range(100, 10000):
  for j in str(i):
    num = str(i)
    num1 = num[0]
    num2 = num[1]
    num3 = num[2]
  if num1 != num2 and num1 != num3 and num2 != num3:
    counter += 1
print(f"Количество целых чисел в диапазоне от 100 до 9999 у которых все цифры разные составляет: {counter}")
