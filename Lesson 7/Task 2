Задание 2 Подсчитать количество целых чисел в диапазоне от 100 до 999 у которых есть две одинаковые цифры.

counter = 0 #счетчик

for i in range(100, 1000):
  for j in str(i):
    num = str(i)
    num1 = num[0]
    num2 = num[1]
    num3 = num[2]
  if num1 == num2 or num1 == num3 or num2 == num3:
    counter += 1
print(f"Количество целых чисел в диапазоне от 100 до 999 у которых есть две одинаковые цифры составляет: {counter}")
