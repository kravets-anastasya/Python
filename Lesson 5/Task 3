Задание 3
Пользователь вводит с клавиатуры два числа
(начало и конец диапазона). Требуется проанализировать все числа в этом диапазоне. Вывод
на экран должен проходить по правилам, указанным ниже.
Если число кратно 3 (делится на 3 без остатка)
нужно вывести слово Fizz. Если число кратно 5
нужно вывести слово Buzz. Если число кратно 3 и
5 нужно вывести Fizz Buzz. Если число не кратно
не 3 и 5 нужно вывести само число.

the_beginning_of_the_range = int(input('Введите начало диапазона: '))
end_of_the_range = int(input('Введите конец диапазона: '))
counter = 0

print('''
Выводиться:"
Fizz Buzz - если число кратно 3 и 5;
Fizz - если число кратно 3;
Buzz - если число кратно 5;
Само число - если число не кратно 3 и 5;
  ''')

for i in range(the_beginning_of_the_range, end_of_the_range + 1):
  counter += 1
  if ((i % 3) == 0) and ((i % 5) == 0):
    print(f"Ваше число в диапазоне №{counter}: Fizz Buzz")
  elif (i % 3) == 0:
    print(f"Ваше число в диапазоне №{counter}: Fizz")
  elif (i % 5) == 0:
    print(f"Ваше число в диапазоне №{counter}: Buzz")
  elif ((i % 3) != 0) and ((i % 5) != 0):
    print(f"Ваше число в диапазоне №{counter}: {i}")
